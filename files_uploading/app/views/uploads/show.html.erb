<h1><%= @upload.title %></h1>

<ul>
  <% @upload.attachments.each do |attachment| %>
    <li><%= link_to attachment.attach.name, attachment.attach.url %></li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_upload_path(@upload) %> |
<%= link_to 'Back', uploads_path %>
